{
  "name": "z33milan",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish": "git push",
    "update": "git pull",
    "connect:server": "ssh -i \"shared/config/keys/ec2-20180302.pem\" ubuntu@ec2-18-197-147-236.eu-central-1.compute.amazonaws.com",
    "connect:gpu": "ssh -i \"shared/config/keys/ec2-us.pem\" ubuntu@ec2-34-227-109-120.compute-1.amazonaws.com",
    "connect:p1": "ssh -t teis@teis.me ssh pi@localhost -p 60122",
    "connect:p2": "ssh -t teis@teis.me ssh pi@localhost -p 60222",
    "connect:p3": "ssh -t teis@teis.me ssh pi@localhost -p 60322",
    "connect:p4": "ssh -t teis@teis.me ssh pi@localhost -p 60422",
    "connect:p5": "ssh -t teis@teis.me ssh pi@localhost -p 60522",
    "connect:cups": "sh shared/config/scripts/cups.sh"
  },
  "author": "Teis S.A. De Greve",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "dotenv": "^6.0.0",
    "eslint": "^4.18.0"
  }
}
